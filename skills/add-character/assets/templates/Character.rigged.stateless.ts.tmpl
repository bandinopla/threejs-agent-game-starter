import type { Object3D } from "three";
import { RiggedEntity, type RigAnimationClips } from "../RiggedEntity";

/** Asset and spawn work is tracked in docs/character-integrations/__FILE_STEM__.md. */
export class __CLASS_NAME__ extends RiggedEntity {
	constructor(rig: Object3D, clips: RigAnimationClips) {
		super(rig, clips);
	}
}
